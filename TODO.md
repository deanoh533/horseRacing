# 📋 TODO — 우선순위별 할일

> 마지막 정리: 2026-08-23 (운영 섹션 신설). 의문·검토 단계는 troubleshooting.md에 남기고, 결정 후 여기로 옮깁니다.

---

## 🟡 P1 — UX / 데이터 보강

### UI/UX 개선

- [ ] **E-004 트랙 이동 이력** — 서울↔부경 이동 말 마킹 (적응 기간 고려)
- [ ] **E-005 연속 완주 여부** — 최근 N경주 모두 완주 여부 (낙마·심정지 이력 경계)

### 신규 기능

- [x] **F-001 페이스 예측** — 완료 2026-07-16: RacePaceBadge(출마정보+/picks), 서버 computePaceType 동일 규칙 + 실측 해석 문구. 신호(모델 피처) 트랙은 기각 종결(docs/status/04-signals.md) — 순수 UI로 구현.
- [ ] **F-003 사용자 메모/별표** — 경주·말별 개인 메모 및 즐겨찾기 기능
- [x] **F-004 H7 교차표 UI 노출** — 완료 2026-07-18: /insights 전개 인사이트 페이지, 정적 12칸 실측표(export:h7). 말별 매핑은 서버 저장 필요 — 후속 검토
- [ ] **F-005 사후 리뷰 도구** — 경주 후 전개 복기 (전개 스펙 §7 이월. v7 라이브 결과 쌓이는 주말 이후 시기 적합)

- [ ] **PRD v6.1 Phase 2** — HorseDetail 18항목 점수 → 원시값+맥락 표현
- [ ] **T-011 PRD legend derived 5개**
  - ⑨ 마주의 금일 출주두수
  - ⑩ 출전경주와 마필
  - ⑬ 최근 3개월 성적
  - ⑮ 출주간격
  - ⑯⑰ 조교사 통계
  - 방식: SQL view 신설 또는 client-side aggregation
- [ ] **T-012 ㉚ 절대능력지수 — KRA 등급변동 API (#15058076) 조사**
  - probe 스크립트 작성 → 가용성·스키마 확인
- [ ] **T-005 ⑧⑭ 전문가 자문 후 산식 교체**
  - ⑧ 부담중량: ρ=0.321로 이미 강함. 핸디캡=능력proxy 메커니즘 이해 후 개선
  - ⑭ 혈통: 현재 데이터 없음(null). 활성화 전 데이터 확보 필요

## 🟢 P2 — 정리·기술 부채

- [ ] **T-009 적중률 분리 (출전마수·경마장·등급별)**
- [ ] **T-013 외부 데이터 출처 검토** (조교상태·마필가격·복기평·경주로 빠르기)

## 🚀 운영 직전 필수 — 런치 게이팅 항목

> 이 섹션이 전부 완료돼야 실사용(베팅 참고) 전환 가능.

- [x] **L-001 predictions 보존 전략 (v7 라이브 적중률 추적)** — 완료 2026-07-11
  - 원래 설계는 `prediction_logs` 불변 로그 테이블 신설이었으나, **기존 `predictions` 테이블의 쓰기 경로만
    바꿔 대체 구현**함 (스키마 변경 없음): `dailySync`가 사전 예측(수요일)을 재계산하지 않고 보존, 금요일
    결과 도착 시 `predictions.actual_ord`만 UPDATE(예측값 필드 불변). 예측 없는 경주는
    `forcePrecompetition`으로 사전 모드 보충 INSERT.
  - 판정: `npm run probe:v7-accuracy` (`src/engine/eval/v7Accuracy.ts` + `scripts/probe_v7_accuracy.ts`)
  - 설계: `docs/superpowers/specs/2026-07-11-v7-live-tracking-design.md` · 계획: `docs/superpowers/plans/2026-07-11-v7-live-tracking.md`
  - 문서: `docs/accuracy_metrics.md §8.6`, `docs/prediction_mode.md §8`, `docs/data_flow.md`, `docs/status/02-model-benchmark.md`

- [x] **L-002 sync 자동화 스케줄링** — 완료 2026-07-12
  - GitHub Actions `.github/workflows/sync.yml`: 출마표 수·목·금 15:00 KST(`sync:cards`, 날짜 기본값=오늘+2일 코드 추가) / 결과 토·일·월 01:00 KST(`sync`, 어제 기본값). `workflow_dispatch`로 수동 재실행(날짜 입력 가능).
  - 함정 처리: 러너 UTC → `TZ: Asia/Seoul` / 러너에 미러 없음 → `DB_SOURCE: supabase`.

- [x] **L-003 가중치 재학습 주기 정책 결정** — 완료 2026-07-12 (정책 문서화)
  - **v7 라이브 1개 분기(약 12주) 누적 + probe:v7-accuracy 첫 판정까지 재학습·승격 동결.** 이후 분기 1회 수동 사이클: `db:snapshot` → `learn:candidate` → `db:pull --table model_versions` → `benchmark` → 사용자 판단 → `promote`. 자동 재학습·자동 승격 없음.

- [x] **L-004 에러 알림 채널** — 완료 2026-07-12
  - 워크플로우 실패 → GitHub 이메일 자동. `--fail-on-empty`로 "성공인데 0건" 조용한 실패도 실패 처리(휴장일 오탐은 확인 후 무시).

- [x] **L-005 DB 백업·복구 계획** — 완료 2026-07-12
  - `npm run db:snapshot`(predictions → predictions_snapshot_YYYYMMDD, DB 내부 복사·egress 0, `--force`/`--prune N`). 복원 SQL·db:pull 미러 복원 개요는 docs/pipeline_guide.md.
  - 설계: `docs/superpowers/specs/2026-07-11-launch-gating-ops-design.md`

---

## 🔥 운영 — 무인 sync 안정화 (2026-08 진행 중)

> 무인 cron 진입(7/15) 후 실제로 터진 것들. 상세 → [docs/status/05-data-infra.md](docs/status/05-data-infra.md)

- [x] **O-001 결과 구멍 재싱크** — 완료 2026-08-28
  - 실행: `npm run sync -- --date 20260823` / `20260822` / `20260815` / `20251226` / `20260627` / `20260628`
    (뒤 2건은 O-002로 처음 감지 — `probe:sync-health --from 20251220` 전체 스캔에서 발견, 이전엔 8/1 이후만 봤어서 놓침).
  - 결과: **20260823·20260822·20260815·20260627·20260628 전부 ✅ 정상 확정.** 20260823은
    재싱크 시 KRA가 즉시 응답. ⚠️ 처음엔 "그날 한정 장애"로 판단했으나 **오판정** — Actions 이력을
    더 보니 8/27 16:52 UTC 출마표 sync(API26_2, 결과와 다른 엔드포인트)도 같은 타임아웃 패턴으로
    실패했다. **8/23~8/27 사이 간헐적으로 재발한 장애**(8/26 06:22 UTC는 정상)이지 하루짜리도
    지속형도 아니다 — 자세한 내용은 O-003 갱신 참고.
  - **20251226 부경 R6은 영구 미해결로 남는다** — 재싱크해도 KRA가 `⏭ 미시행·결과 미확정 → 스킵`을
    반환한다(실제 취소 경주, 착순 데이터 자체가 존재하지 않음). `dailySync`의 미시행 가드는
    `ord>0` 유무만 보고 "일시적 지연 vs 영구 취소"를 구분할 신호가 KRA 응답에 없다(`ordBigo` 등
    상태 필드 미제공) → **probe:sync-health가 이 날짜를 앞으로도 계속 ❗ gap으로 표시한다.**
    한 건짜리 예외로 판정 로직에 휴리스틱을 넣지 않고 알려진 예외로만 남겨둠 (아래 참고).

- [x] **O-002 probe:sync-health 부분 결과 감지** — 완료 2026-08-28 (main 머지 6267186)
  - 기존 `classifyRaceDate`는 `ordFilled === 0`만 구멍으로 봐서 **10경주 중 8경주만 온 8/22·8/15가 ✅ 정상**이었다.
  - `ordFilled < entries`는 제외마 때문에 매일 오탐 → **경주 단위 대조**로 교체: `RaceDateCounts.racesWithResult`
    (결과 있는 meet-rc_no distinct) < `races` → 새 상태 `gap`(❗ 경주 일부 구멍), 백필 명령 목록에 hole과 함께 포함.
  - distinct는 PostgREST가 못 하므로 (경주일·경마장·경주번호)만 페이지네이션으로 받아 센다.
  - 파일: `src/utils/syncHealth.ts` + `.test.ts`(12건), `scripts/probe_sync_health.ts`. 표에 `결과경주` 컬럼 추가.
  - 실측 검증: 8/15·8/22가 ❗로 잡히고 두수만 1~2 모자란 날(0807·0821 등)은 ✅ 유지 — 오탐 없음.

- [x] **O-003 결과 sync 재시도 전략 개선** — 완료 2026-08-29 (브랜치 feat/results-poller)
  - 2026-08-22은 워크플로 푸시(23:56 KST)가 크론(23:00 KST)보다 늦어 2차 슬롯이 안 돌았다.
  - **2026-08-23(일) 첫 2슬롯 가동 → 두 슬롯 모두 실패**(run 32633753434 / 32644963163).
    `API214_1` **120초 무응답 × 5회 × 2경마장**, 서울·부경 동시. 부분 응답도 5xx도 아님.
  - **타임라인 재구성(2026-08-28, Actions 이력 전체 대조)** — 처음엔 "그날 한정 장애"로 결론냈으나
    이후 로그를 더 보니 **오판정**이었다:

    | 시각(UTC) | 잡 | 엔드포인트 | 결과 |
    |---|---|---|---|
    | 8/23 10:26 | 결과 | API214_1 | ❌ 타임아웃 |
    | 8/23 14:17 | 결과 | API214_1 | ❌ 타임아웃 |
    | 8/26 06:22 | 출마표 | API26_2 | ✅ 정상(재시도 없이 즉시) |
    | 8/27 16:52 | 출마표 | API26_2 | ❌ 같은 타임아웃 패턴 |
    | 8/28 (수동 백필) | 결과 | API214_1 | ✅ 정상 |

    **8/23~8/27 사이 간헐적으로 재발한 KRA 장애**다 — 8/26엔 멀쩡했으니 지속형(계속 다운)도 아니고,
    8/27에 다시 걸렸으니 하루짜리 블립도 아니다. 또한 **결과(API214_1)뿐 아니라 출마표(API26_2)도
    같이 걸렸다** — 우리 쪽 특정 엔드포인트 문제가 아니라 KRA 서버 전반의 간헐적 불안정.
  - 확정된 것: **타임아웃 60→120초 상향은 이 장애 유형에 효과가 없었다.** 재시도 백오프가
    1→2→4→8초라 5회 총 스팬이 ~10분뿐 — 몇 시간~며칠 단위로 재발하는 장애는 슬롯 내 재시도로
    못 넘긴다. **슬롯을 하루 안에서 늘리는 방향(3차 슬롯·백오프 확대)은 간헐적 다일 장애엔
    근본적으로 안 맞는다** — 다음날도 실패할 수 있기 때문.
  - 재검토된 선택:
    ① 결과 sync에 **일별 자동 캐치업**을 추가 — 매일(요일 무관) `probe:sync-health` 판정 로직으로
       최근 N일 중 hole·gap이 남은 날짜를 찾아 자동 재싱크. 간헐적 장애가 며칠 뒤 풀렸을 때 사람
       개입 없이 자동 복구됨. (오늘 한 일을 스케줄로 자동화하는 것과 같음)
    ② 3차 슬롯/백오프 확대 — 근거 약함(위 이유로 기각 권고)
    ③ 현행 유지(수동 백필 안전망) — probe가 정확해졌으니 최소 요건은 충족하지만 사람이 주기적으로
       돌려야 함
  - **채택: ①을 넘어서 결과 수신 방식 자체를 재설계** — 사용자 요청으로 "발주시각(출마표) 체크해서
    경주 끝났을 때 결과를 가져오는" 방식으로 19시·23시 고정 슬롯을 **완전히 대체**.
  - **구조(2개 잡)**:
    1. **결과 폴러**(`scripts/resultsPoll.ts`, `npm run sync:poll`) — 경주 있는 날 KST 10:00~21:45,
       15분 간격(cron `*/15 1-12 * * 5,6,0`). 매 폴마다 `races.st_time`(발주시각)만 가볍게 조회해
       발주시각+15분이 지났는데 착순이 없는 경주가 있을 때만 KRA를 부른다. 없으면 KRA 호출 0.
       순수 판정 로직 `src/sync/resultsPollLogic.ts`(11 테스트) — `parseStTime`(원문 "출발 :10:35" 파싱)
       + `hasDueUnsyncedRace`. 너무 일찍 불러도 기존 "미시행 가드"가 스킵해 안전.
    2. **캐치업**(`scripts/catchupSync.ts`, `npm run sync:catchup`) — 폴러와 독립, 매일 KST 07:00
       (cron `0 22 * * *`). 최근 7일 중 hole·gap 날짜를 `classifyRaceDate`로 찾아 자동 재싱크.
       7일이 지나면 오래된 진짜 취소 경주(20251226 R6류)는 창에서 저절로 빠져 무한 재시도 없음.
  - **알림 정책(L-004 유지)**: 캐치업이 시도해도 **2일 이상 묵은 구멍이 여전히 0건**이면 그때만
    실패 처리(메일). 어제·그저께 생긴 구멍은 조용히 넘어감(정상 지연) — 매 폴 실패마다 메일 오던
    옛 방식보다 신호 대 소음비 개선. 순수 판정 `src/sync/catchupLogic.ts`(`isStaleUnresolved`, 3 테스트).
  - **퍼블릭 레포 확인**: `gh repo view` → PUBLIC, Actions 분 무제한 — 15분 간격 폴링의 비용 우려 해소.
  - **DB 조회 중복 제거**: `probe_sync_health.ts`와 `catchupSync.ts`가 쓰던 카운트 조회를
    `src/sync/syncHealthQuery.ts`(`fetchRaceDateCounts`)로 추출·공유.
  - 검증: `tsc` 통과, 전체 테스트 602 passed(신규 14건), `--dry-run`으로 실제 DB 대조 스모크 테스트 완료.
  - 문서: CLAUDE.md 한 주의 흐름, `.github/workflows/sync.yml` 갱신.


- [x] **O-006 KRA 타임아웃 값 재조정** — 완료 2026-08-30
  - O-003이 "120초 상향은 효과 없었다"까지 확인했고, 그 뒤 원인을 더 좁혔다.
  - **정상 응답은 1~4초다**(Actions 로그 실측: 8/28 23:22:05→09 = 3.7초, 21:07:52→53 = 1.3초).
    그런데 120초×5회를 기다려 경마장당 10분·양쪽 20분을 태우고 있었다. **30배 여유를 줘도
    실패한다는 건 "느려서"가 아니라 "응답이 아예 안 온다"는 뜻** — 4xx도 5xx도 RST도 없이 침묵.
  - **핵심 반증(2026-08-21)**: 같은 명령·같은 날짜(20260821)·같은 엔드포인트(API26_2)인데
    03:55 실행은 60초×4회 전멸, **3분 뒤 03:58 실행은 73초 만에 88마 수집 성공**.
    3분 사이 KRA가 죽었다 살아났다고 보기 어렵다 — 바뀐 변수는 러너 VM(=출발지 IP)뿐.
    → O-003의 "KRA 서버 전반 불안정" 판정은 8/23~27 구간엔 맞지만, **모든 에피소드가 서버측인
    건 아니다.** 최소한 일부는 출발지(커넥션/IP) 스코프다.
  - **지역은 원인 아님**: `eastus`가 실패(8/21)·성공(8/22) 양쪽에, `westus3`도 성공(8/21)·실패(8/23)
    양쪽에 나온다. Azure 지역 단위 차단이 아니라 개별 러너 IP 단위로 갈린다.
  - **공공데이터포털 http(80) 공지는 우리 케이스 아님**(2026-08-29 확인): 코드의 data.go.kr
    호출 5곳 전부 이미 https이고 평문 http는 저장소 전체 0건. 공지 장애 구간(8/4~8/5 09시 조치완료)도
    우리 실패(8/7~)보다 앞선다. 다만 **"포털 앞단 보안 조치가 호출을 조용히 죽인 전례"**라는
    방증은 된다 — 그때 기준은 포트, 우리 건 출발지 IP로 보인다.
  - **조치**: 타임아웃 120→**30초**(정상치의 약 8배), 시도 4회 유지, **keep-alive off**
    (Node 19+ 전역 agent는 keepAlive 기본 on이라 응답 끊긴 소켓을 물고 재시도하면 매 시도가
    같은 조건 — 시도마다 새로 dial하게 해 최소한 커넥션은 바뀌게 한다). 워크플로 `timeout-minutes`
    racecard 90→30. 기본값 회귀 테스트 3건 추가(tests/kra/client.test.ts).
  - **효과**: 무응답 구간에서 태우는 시간이 경마장당 10분 → **2분**. 구멍 복구는 이미
    캐치업(새 러너 = 새 IP)이 맡고 있으므로 슬롯 안에서 오래 버틸 이유가 없다.
  - **남은 확인**: "해외 IP라서"를 확정하려면 러너가 실패하는 그 순간 한국 로컬에서 같은 요청이
    되는지 동시 대조가 필요하다(지금까진 몇 시간 뒤 백필이라 동시성 없음). 확정되면 KRA 문의 근거가 된다.
- [ ] **O-004 v7 라이브 적중률 첫 판정** — 재학습 동결 해제 조건 (L-003)
  - v7-shape 승격 2026-07-10 + 1개 분기(~12주) → **2026년 10월 초** 시점.
  - `npm run probe:v7-accuracy` (⚠️ `db:pull` 선행). 이후 분기 1회 수동 재학습 사이클 재개.

- [ ] **O-005 (관찰) 경주 전 조합배당 = 실시간 배당인가**
  - 2026-08-23 발견: `API160_1`이 **아직 시행 안 된 경주에도 조합배당을 내려줬다**(8/22 R9 504건·R10 1375건 = 발매 중 예상배당).
  - 사실이면 [[project_odds_blend_candidate]]가 "라이브 배당 API 없음"으로 보류한 전제가 흔들린다(검증 당시 연승 +7~11.5%p 양성).
  - ⚠️ 단정 금물: R10은 단승 배당 0인데 조합배당은 존재 등 결이 일정치 않음 → **경주 전 시각에 직접 호출해 값이 변하는지** 확인이 선행.
  - 같은 관찰이 P3 "당일 결과 폴링"의 선행 확인도 일부 답한다: 8/22 19시 시점에 R1~R8 결과가 이미 있었으므로 **결과는 밤 일괄이 아니라 경주별로 순차 반영된다.**

---

## 🔮 P3 — 향후 확장 (백로그)

- [ ] **AI 인사이트** — 각 경주에 Claude API 코멘트 (PRD Phase 2)
- [ ] **PDF 분석 보고서** — 경주별 자동 생성
- [ ] **유튜브 대본 자동 생성** — 경주별
- [ ] **win_odds 시계열 캡처** — 경주 직전 변동 추적
- [x] **당일 결과 폴링** — 완료 2026-08-29, O-003으로 구현(아래 참고). 선행 확인(결과가 경주별 순차 반영)은 O-005 관찰로 이미 답이 나왔었음. 우려했던 Actions 무료한도는 레포가 **퍼블릭이라 무제한**이라 기각.

---

## 의문 (해결 필요)

- [ ] **Q-001** ⑤ "후반 구간"의 시작점 정의 (g3f vs g1f vs 둘 다)
- [ ] **Q-002** Spearman 학습 윈도우 (전 기간 vs 최근 1년 vs 슬라이딩)
- [ ] **Q-003** 가중치 학습 적용 빈도 (수동 vs 주기적 vs 임계점)
