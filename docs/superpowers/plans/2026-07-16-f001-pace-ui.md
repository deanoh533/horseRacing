# F-001 페이스 예측 UI (RacePaceBadge) 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 경주 단위 페이스 예상(🔥 접전/➖ 보통/🐢 느린 전개) 배지를 출마정보(RaceEntries)와 /picks(TodayPicks)에 노출한다 — 근거(선두권 N마리) + 실측 해석 1줄 포함.

**Architecture:** 순수 계산은 `client/src/lib/pace.ts`(서버 `computePaceType`과 동일 규칙 + 표시 전용 커버리지 가드), 표시는 `client/src/components/RacePaceBadge.tsx` 하나로 두 화면 재사용. RaceEntries는 기존 abilities 데이터 재사용(쿼리 0), TodayPicks는 배치 훅 1개 신설(+기존 abilities 훅 재사용, 쿼리 2).

**Tech Stack:** React + TypeScript + Tailwind (client/), TanStack Query(기존 패턴), 루트 vitest(클라 순수함수 테스트 — `client/src/pages/PredictionSheet.test.ts` 선례).

## Global Constraints

- 스펙: `docs/superpowers/specs/2026-07-16-f001-pace-ui-design.md`. 이 기능은 **순수 UI** — 서버/엔진/DB 변경 절대 금지.
- 판정 규칙(스펙 §3, 서버 `src/engine/scorePredictor.ts:computePaceType`과 동일): 선두권(front|pace, 자유마·unknown 제외) **≥3 → HOT / ≤1 → SLOW / 그 외 NORMAL**. 양쪽에 상호 참조 주석 필수.
- 커버리지 가드(스펙 §2, 표시 전용): 성향 데이터 보유 말(knownCount) < 출전마 절반이면 판정 불가(null) → "페이스 판정 불가 — 성향 데이터 부족".
- 해석 문구(스펙 §4 — 그대로 사용, 임의 수정 금지):
  - HOT: "접전 경주 실측: 도주마 승률 21% vs 추입마 4% — 선두권이 오히려 유리했던 게 실측"
  - NORMAL: "보통 전개 실측: 선행마가 가장 안정적, 추입마는 평균 이하"
  - SLOW: "느린 전개 실측: 추입마 최악(막판 가속 여지 없음), 선입마 유리"
- 색 관례: `STYLE_INFO`(runningStyle.ts) 패턴 `bg-*-500/20 text-*-300 border-*-500/40`.
- 각 커밋 전: `npm run test:run`(루트) + `cd client && npx tsc -b`(클라 타입체크) 통과.
- 커밋 메시지 한국어 + scope, 끝에 트레일러:
  Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_019mNxwWBSj5TPozmoznJ4BA
- 브랜치: `feat/f001-pace-ui` (스펙 커밋 5ddcb78 위에 쌓기).

---

### Task 1: lib/pace.ts — 페이스 판정 순수 계산 + 문구 상수 (TDD)

**Files:**
- Create: `client/src/lib/pace.ts`
- Test: `client/src/lib/pace.test.ts`

**Interfaces:**
- Consumes: `type RunningStyle`(`client/src/lib/runningStyle.ts` — 'free'|'front'|'pace'|'stalker'|'closer'|'unknown')
- Produces (Task 2·3이 그대로 import):
  - `type PaceType = 'HOT' | 'NORMAL' | 'SLOW'`
  - `interface RacePaceResult { paceType: PaceType; frontCount: number; knownCount: number; total: number }`
  - `computeRacePace(styles: RunningStyle[]): RacePaceResult | null` — null = 판정 불가
  - `PACE_UI: Record<PaceType, { emoji: string; label: string; insight: string; className: string }>`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// client/src/lib/pace.test.ts
import { describe, it, expect } from 'vitest';
import { computeRacePace, PACE_UI } from './pace';
import { classifyRunningStyle, type RunningStyle } from './runningStyle';

const S = (s: string) => s as RunningStyle;

describe('computeRacePace — 서버 computePaceType 동일 규칙', () => {
  it('선두권(front|pace) 3마리 이상 → HOT', () => {
    const r = computeRacePace([S('front'), S('pace'), S('pace'), S('stalker'), S('closer')]);
    expect(r).toEqual({ paceType: 'HOT', frontCount: 3, knownCount: 5, total: 5 });
  });
  it('선두권 1마리 이하 → SLOW, 2마리 → NORMAL', () => {
    expect(computeRacePace([S('front'), S('stalker'), S('closer'), S('closer')])?.paceType).toBe('SLOW');
    expect(computeRacePace([S('front'), S('pace'), S('stalker'), S('closer')])?.paceType).toBe('NORMAL');
  });
  it('자유마는 선두권 카운트 제외 (서버 isFree 제외와 동치)', () => {
    // free 2 + front 2 → frontCount 2 → NORMAL (free가 카운트되면 HOT이 됐을 것)
    const r = computeRacePace([S('free'), S('free'), S('front'), S('front'), S('stalker'), S('closer')]);
    expect(r?.paceType).toBe('NORMAL');
    expect(r?.frontCount).toBe(2);
  });
  it('unknown은 knownCount에서 제외, 절반 미만이면 판정 불가(null)', () => {
    // 6두 중 known 2 (< 3 = 절반) → null
    expect(computeRacePace([S('front'), S('pace'), S('unknown'), S('unknown'), S('unknown'), S('unknown')])).toBeNull();
    // 6두 중 known 3 (= 절반, 미만 아님) → 판정함
    expect(computeRacePace([S('front'), S('pace'), S('pace'), S('unknown'), S('unknown'), S('unknown')])?.paceType).toBe('HOT');
  });
  it('빈 배열 → null', () => {
    expect(computeRacePace([])).toBeNull();
  });
  it('서버 규칙 동치 — (avg,std) 표 기반 대조', () => {
    // 서버 computePaceType(scorePredictor.ts): avg≤0.35 && !(std≥0.35) 카운트, ≥3 HOT / ≤1 SLOW
    const horses: Array<[number | null, number | null]> = [
      [0.10, 0.10], [0.30, 0.20], [0.34, 0.10], [0.50, 0.10], [0.80, 0.10], [0.20, 0.40], // free
    ];
    // 서버 방식 직접 계산
    let serverFront = 0;
    for (const [avg, std] of horses) {
      if (avg == null) continue;
      const isFree = std != null && std >= 0.35;
      if (!isFree && avg <= 0.35) serverFront++;
    }
    const serverPace = serverFront >= 3 ? 'HOT' : serverFront <= 1 ? 'SLOW' : 'NORMAL';
    // 클라 방식: classifyRunningStyle → computeRacePace
    const r = computeRacePace(horses.map(([a, s]) => classifyRunningStyle(a, s)));
    expect(r?.paceType).toBe(serverPace);
    expect(r?.frontCount).toBe(serverFront);
  });
});

describe('PACE_UI', () => {
  it('세 타입 모두 emoji·label·insight·className 보유', () => {
    for (const t of ['HOT', 'NORMAL', 'SLOW'] as const) {
      expect(PACE_UI[t].emoji).toBeTruthy();
      expect(PACE_UI[t].label).toBeTruthy();
      expect(PACE_UI[t].insight).toContain('실측');
      expect(PACE_UI[t].className).toMatch(/bg-.+text-.+border-/s);
    }
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run client/src/lib/pace.test.ts`
Expected: FAIL — "Cannot find module './pace'"

- [ ] **Step 3: 구현**

```ts
// client/src/lib/pace.ts
/**
 * 경주 단위 페이스 예상 (F-001) — 순수 계산 + 표시 상수.
 * 판정 규칙은 서버 src/engine/scorePredictor.ts:computePaceType 과 동일해야 한다
 * (선두권 = front|pace = avg≤0.35 & 자유마 제외, ≥3 HOT / ≤1 SLOW / 그 외 NORMAL).
 * 서버 규칙 변경 시 이 파일과 pace.test.ts의 동치 테스트를 함께 갱신할 것.
 * 스펙: docs/superpowers/specs/2026-07-16-f001-pace-ui-design.md
 */
import type { RunningStyle } from './runningStyle';

export type PaceType = 'HOT' | 'NORMAL' | 'SLOW';

export interface RacePaceResult {
  paceType: PaceType;
  frontCount: number; // 선두권(도주+선행, 자유마 제외) 마릿수
  knownCount: number; // 성향 데이터 보유 말 수
  total: number;      // 전체 출전마 수
}

/**
 * 출전마 성향 배열 → 경주 페이스 예상.
 * null = 판정 불가 (성향 데이터 보유 말이 절반 미만 — 표시 전용 가드, 산식 아님).
 */
export function computeRacePace(styles: RunningStyle[]): RacePaceResult | null {
  const total = styles.length;
  if (total === 0) return null;
  let frontCount = 0;
  let knownCount = 0;
  for (const s of styles) {
    if (s === 'unknown') continue;
    knownCount++;
    if (s === 'front' || s === 'pace') frontCount++;
  }
  if (knownCount < total / 2) return null;
  const paceType: PaceType = frontCount >= 3 ? 'HOT' : frontCount <= 1 ? 'SLOW' : 'NORMAL';
  return { paceType, frontCount, knownCount, total };
}

/** 해석 문구는 ⑲ 실측(2026-06-16, SCORE_MAP 교정 데이터) 기반 — 스펙 §4 고정 문구. */
export const PACE_UI: Record<PaceType, { emoji: string; label: string; insight: string; className: string }> = {
  HOT: {
    emoji: '🔥',
    label: '접전 예상',
    insight: '접전 경주 실측: 도주마 승률 21% vs 추입마 4% — 선두권이 오히려 유리했던 게 실측',
    className: 'bg-red-500/20 text-red-300 border-red-500/40',
  },
  NORMAL: {
    emoji: '➖',
    label: '보통 전개',
    insight: '보통 전개 실측: 선행마가 가장 안정적, 추입마는 평균 이하',
    className: 'bg-zinc-500/20 text-zinc-300 border-zinc-500/40',
  },
  SLOW: {
    emoji: '🐢',
    label: '느린 전개 예상',
    insight: '느린 전개 실측: 추입마 최악(막판 가속 여지 없음), 선입마 유리',
    className: 'bg-sky-500/20 text-sky-300 border-sky-500/40',
  },
};
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run client/src/lib/pace.test.ts && cd client && npx tsc -b && cd ..`
Expected: 8 tests PASS + 타입 에러 0

- [ ] **Step 5: 커밋**

```bash
git add client/src/lib/pace.ts client/src/lib/pace.test.ts
git commit -m "feat(ui): 경주 페이스 판정 순수 계산 lib/pace.ts — 서버 computePaceType 동일 규칙 + 커버리지 가드 (F-001)"
```

---

### Task 2: RacePaceBadge 컴포넌트 + 출마정보(RaceEntries) 부착

**Files:**
- Create: `client/src/components/RacePaceBadge.tsx`
- Modify: `client/src/pages/RaceEntries.tsx` (styleByName 계산 103~109행 아래에 paceStyles 추가, 165행 `<RaceInfoBlock …/>` 아래에 배지 렌더)

**Interfaces:**
- Consumes: `computeRacePace`, `PACE_UI` (Task 1) / RaceEntries의 기존 `hrNames`·`styleByName`(Map<string, RunningStyle>)
- Produces: `RacePaceBadge({ styles }: { styles: RunningStyle[] })` — Task 3이 동일 컴포넌트 재사용

- [ ] **Step 1: 컴포넌트 작성**

```tsx
// client/src/components/RacePaceBadge.tsx
import { computeRacePace } from '../lib/pace';
import { PACE_UI } from '../lib/pace';
import type { RunningStyle } from '../lib/runningStyle';

/**
 * 경주 페이스 예상 배지 (F-001): 배지 + 근거(선두권 N마리) + 실측 해석 1줄.
 * 판정 불가(성향 데이터 절반 미만)면 회색 안내. 스타일 선례: PickBadge.tsx.
 */
export function RacePaceBadge({ styles }: { styles: RunningStyle[] }) {
  const pace = computeRacePace(styles);
  if (pace === null) {
    return (
      <div className="text-xs text-[var(--color-text-disabled)]">
        페이스 판정 불가 — 성향 데이터 부족
      </div>
    );
  }
  const ui = PACE_UI[pace.paceType];
  return (
    <div className="flex flex-col gap-0.5">
      <div className="flex items-center gap-2">
        <span className={`inline-block px-1.5 py-0.5 rounded border text-[11px] font-semibold leading-none ${ui.className}`}>
          {ui.emoji} {ui.label}
        </span>
        <span className="text-xs text-[var(--color-text-secondary)]">
          선두권 {pace.frontCount}마리 <span className="text-[var(--color-text-disabled)]">({pace.knownCount}/{pace.total}두 분석)</span>
        </span>
      </div>
      <p className="text-[11px] text-[var(--color-text-disabled)]">{ui.insight}</p>
    </div>
  );
}
```

- [ ] **Step 2: RaceEntries에 부착**

`client/src/pages/RaceEntries.tsx` 수정 2곳:

(a) import 추가 (기존 import 블록에):

```tsx
import { RacePaceBadge } from '../components/RacePaceBadge';
```

(b) `styleByName` useMemo(103~109행) **바로 아래**에 추가:

```tsx
  // F-001: 경주 페이스 예상 — 출전마 전원의 성향 배열 (데이터 없는 말은 unknown)
  const paceStyles = useMemo(
    () => hrNames.map((n) => styleByName.get(n) ?? ('unknown' as RunningStyle)),
    [hrNames, styleByName]
  );
```

(c) `<RaceInfoBlock …/>`(165행) 바로 아래, "AI 예측 요약" 블록 위에 렌더:

```tsx
      {/* F-001: 경주 페이스 예상 */}
      {hrNames.length > 0 && (
        <div className="bg-[var(--color-bg-surface)] rounded-xl p-3 border border-[var(--color-bg-elevated)]">
          <RacePaceBadge styles={paceStyles} />
        </div>
      )}
```

주의: `RunningStyle` 타입은 이 파일에 이미 import돼 있음(33행) — 재선언 금지.

- [ ] **Step 3: 검증**

Run: `cd client && npx tsc -b && cd .. && npm run test:run`
Expected: 타입 에러 0 + 전체 테스트 통과(기존 + pace 8)

- [ ] **Step 4: 커밋**

```bash
git add client/src/components/RacePaceBadge.tsx client/src/pages/RaceEntries.tsx
git commit -m "feat(ui): RacePaceBadge 컴포넌트 + 출마정보 화면 부착 — 기존 abilities 재사용(쿼리 0) (F-001)"
```

---

### Task 3: /picks(TodayPicks) 부착 — 출전마 명단 배치 훅 신설

**Files:**
- Modify: `client/src/lib/queries.ts` (useHorseSectionalAbilityByNames 612행 아래에 훅 추가)
- Modify: `client/src/pages/TodayPicks.tsx`

**Interfaces:**
- Consumes: `RacePaceBadge`(Task 2), `computeRacePace`는 사용 안 함(컴포넌트 내부에서 처리), `classifyRunningStyle`·`useHorseSectionalAbilityByNames`(기존)
- Produces: `useRaceEntryNamesByDate(raceDate: number | null)` → `{ race_date, meet, rc_no, hr_name }[]`

- [ ] **Step 1: queries.ts에 훅 추가** (`useHorseSectionalAbilityByNames` 아래)

```ts
/**
 * 특정 날짜 전체 출전마 명단 (F-001 /picks 페이스 배지용 — 경주별 성향 집계에 전체 명단 필요).
 * 픽 없는 날은 raceDate null → 쿼리 스킵.
 */
export function useRaceEntryNamesByDate(raceDate: number | null) {
  return useQuery({
    queryKey: ['race-entry-names', raceDate],
    queryFn: async (): Promise<Array<{ race_date: number; meet: number; rc_no: number; hr_name: string }>> => {
      const { data, error } = await supabase
        .from('race_entries')
        .select('race_date, meet, rc_no, hr_name')
        .eq('race_date', raceDate!);
      if (error) throw error;
      return data ?? [];
    },
    enabled: raceDate != null,
    staleTime: 10 * 60 * 1000,
  });
}
```

- [ ] **Step 2: TodayPicks.tsx에 부착**

전체 구조 주의: **훅은 조기 return(14행 `if (isLoading)`)보다 위에서 무조건 호출**해야 한다 (React 훅 규칙). 수정본:

```tsx
// client/src/pages/TodayPicks.tsx — 상단 import 교체/추가
import { Link } from 'react-router-dom';
import { useMemo } from 'react';
import { useUpcomingPicks, useRaceEntryNamesByDate, useHorseSectionalAbilityByNames } from '../lib/queries';
import { classifyPick } from '../lib/selectivePicks';
import { PickBadge } from '../components/PickBadge';
import { RacePaceBadge } from '../components/RacePaceBadge';
import { classifyRunningStyle, type RunningStyle } from '../lib/runningStyle';
import { fmtPct } from '../lib/sectional';
import type { Prediction } from '../lib/supabase';
```

컴포넌트 본문 — `useUpcomingPicks()` 호출(12행) 직후, `if (isLoading)` **이전**에 추가:

```tsx
  // F-001: 경주 페이스 배지 — 픽 경주의 전체 출전마 성향 필요 (픽 행만으론 집계 불가)
  const picksDate = data?.[0]?.race_date ?? null;
  const { data: entryNames } = useRaceEntryNamesByDate(picksDate);
  const allNames = useMemo(() => [...new Set((entryNames ?? []).map((e) => e.hr_name))], [entryNames]);
  const { data: abilities } = useHorseSectionalAbilityByNames(allNames);
  const stylesByRace = useMemo(() => {
    const styleByName = new Map<string, RunningStyle>();
    (abilities ?? []).forEach((a) => {
      styleByName.set(a.hr_name, classifyRunningStyle(a.avg_position_ratio, a.stddev_position_ratio));
    });
    const map = new Map<string, RunningStyle[]>();
    for (const e of entryNames ?? []) {
      const k = `${e.race_date}-${e.meet}-${e.rc_no}`;
      if (!map.has(k)) map.set(k, []);
      map.get(k)!.push(styleByName.get(e.hr_name) ?? 'unknown');
    }
    return map;
  }, [entryNames, abilities]);
```

경주 카드 렌더(46~55행) — `<Link …>…</Link>` 바로 아래에 추가:

```tsx
            {stylesByRace.has(key) && (
              <div className="mt-1.5">
                <RacePaceBadge styles={stylesByRace.get(key)!} />
              </div>
            )}
```

- [ ] **Step 3: 검증**

Run: `cd client && npx tsc -b && cd .. && npm run test:run`
Expected: 타입 에러 0 + 전체 테스트 통과

- [ ] **Step 4: 커밋**

```bash
git add client/src/lib/queries.ts client/src/pages/TodayPicks.tsx
git commit -m "feat(ui): /picks 경주 카드에 페이스 배지 — 출전마 명단 배치 훅 신설(쿼리 2, 픽 없는 날 스킵) (F-001)"
```

---

### Task 4: 문서 갱신 + 최종 검증

**Files:**
- Modify: `TODO.md` (F-001 항목 완료 처리)
- Modify: `docs/status/06-ui.md` (현재 상태에 1줄 추가, 다음 후보에서 F-001 제거)

**Interfaces:** 없음 (문서만)

- [ ] **Step 1: TODO.md — F-001 체크 + 완료 주석**

16~17행의 F-001 항목을 다음으로 교체:

```markdown
- [x] **F-001 페이스 예측** — 완료 2026-07-16: RacePaceBadge(출마정보+/picks), 서버 computePaceType 동일 규칙 + 실측 해석 문구. 신호(모델 피처) 트랙은 기각 종결(docs/status/04-signals.md) — 순수 UI로 구현.
```

- [ ] **Step 2: docs/status/06-ui.md 갱신**

"현재 상태" 섹션(파일 상단부)에 1줄 추가:

```markdown
- **F-001 페이스 예상 배지 (2026-07-16)** — `lib/pace.ts`(서버 computePaceType 동일 규칙, ≥3 HOT/≤1 SLOW, 데이터 절반 미만 판정불가) + `RacePaceBadge`(배지·선두권 N마리·실측 해석 1줄). 출마정보(쿼리 0)·/picks(배치 2쿼리, `useRaceEntryNamesByDate`). ⚠️ 서버 규칙 변경 시 pace.ts·동치 테스트 동반 갱신.
```

"다음 후보·남음"의 `F-001 페이스 예측(선행마 집계)` 부분을 목록에서 제거 (F-003은 유지).

- [ ] **Step 3: 최종 검증 + 커밋**

Run: `npm run build && npm run test:run && cd client && npx tsc -b && cd ..`
Expected: 전부 통과

```bash
git add TODO.md docs/status/06-ui.md
git commit -m "docs(ui): F-001 완료 처리 — TODO 체크·06-ui 현재상태 갱신"
```

- [ ] **Step 4: 사용자 시각 확인 안내 (보고 항목)**

로컬 확인 명령(사용자 실행): `npm run client:dev` → ① 아무 경주 출마정보 화면에서 배지·근거·해석 표시 ② `/picks`에서 경주 카드 배지 (픽 없는 날이면 표시 없음이 정상) ③ 신마전 등 데이터 부족 경주에서 "판정 불가" 문구. 확인 후 main 머지는 사용자 결정.

---

## Self-Review 결과

- **스펙 커버리지**: §2 위치·깊이·판정불가(Task 2·3, 판정불가는 컴포넌트) / §3 산식·동치(Task 1 표 기반 테스트) / §4 문구 3종(Task 1 상수, 스펙 원문 그대로) / §5 데이터 조달(Task 2 쿼리 0·Task 3 배치 2쿼리+enabled 가드) / §6 구조(파일 3개+훅 1개) / §7 범위 밖 — 서버·DB·PredictionSheet 미접촉 ✓
- **플레이스홀더**: 전 코드 블록 실제 코드 ✓
- **타입 일관성**: `RacePaceResult`·`PaceType`·`PACE_UI`(Task 1 정의 → Task 2 소비), `RacePaceBadge({ styles: RunningStyle[] })`(Task 2 정의 → Task 3 소비), `useRaceEntryNamesByDate(raceDate: number | null)`(Task 3 내 정의·소비) ✓. Task 2의 `import { computeRacePace } from '../lib/pace'; import { PACE_UI } from '../lib/pace';` 두 줄은 한 줄로 합쳐도 무방(구현자 재량).
