# F-004 H7 교차표 /insights 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** probe H9의 12칸 교차표(예측 격차 × 달성확률 → 실측 승률·연승률)를 JSON으로 굳혀 새 페이지 `/insights`에 노출한다.

**Architecture:** 순수 변환·검증은 `src/engine/eval/h7Table.ts`(TDD), SQL 실행·JSON 출력은 `scripts/export_h7_table.ts`(probe H9 SQL 복사, 로컬 DuckDB READ_ONLY). 산출 JSON을 커밋해 클라이언트 번들에 포함 — 런타임 쿼리 0. 화면은 `client/src/pages/Insights.tsx` + 라우트 + 헤더 링크.

**Tech Stack:** TypeScript, `@duckdb/node-api`(probe 선례), React + Tailwind, 루트 vitest.

## Global Constraints

- 스펙: `docs/superpowers/specs/2026-07-18-h7-insights-design.md`. 서버 라이브 경로·DB·모델 변경 금지. `scripts/probe_race_shape.ts`는 **수정 금지**(복사·참조만).
- 버킷 라벨(정확히 이 문자열): 격차 `~0.5초`/`~1.0초`/`~1.5초`/`1.5초+` · 달성확률 `낮음(~30%)`/`중간(30~70%)`/`높음(70%+)`. SQL은 probe의 접두 라벨(`a. ~0.5초`, `1_낮음(~30%)` 등)을 내고 빌더가 클린 라벨로 매핑.
- JSON 검증: 12칸 전부 존재·중복 없음·rate ∈ [0,1]·starts > 0 — 위반 시 스크립트 exit 1.
- 표본 30 미만 칸은 화면에서 흐리게 + "표본 적음".
- 각 커밋 전: `npm run build` + `npm run test:run` + `cd client && npx tsc -b` 통과.
- 커밋 한국어 + scope + 트레일러:
  Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_019mNxwWBSj5TPozmoznJ4BA
- 브랜치: `feat/f004-h7-insights` (스펙 커밋 f208b11 위에).

---

### Task 1: h7Table 빌더(TDD) + export 스크립트 + JSON 생성

**Files:**
- Create: `src/engine/eval/h7Table.ts`
- Test: `src/engine/eval/h7Table.test.ts`
- Create: `scripts/export_h7_table.ts`
- Modify: `package.json` (scripts에 `"export:h7": "tsx scripts/export_h7_table.ts"`)
- Create(스크립트 실행 산출물): `client/src/data/h7_table.json`

**Interfaces:**
- Produces (Task 2가 구조를 그대로 사용):
  - JSON 스키마 = `H7Table`: `{ generatedAt: string; raceDateFrom: number; raceDateTo: number; totalStarts: number; formula: string; cells: H7Cell[] }`
  - `H7Cell`: `{ gapBucket: '~0.5초'|'~1.0초'|'~1.5초'|'1.5초+'; achieveBucket: '낮음(~30%)'|'중간(30~70%)'|'높음(70%+)'; starts: number; winRate: number; placeRate: number }` (rate는 0~1 소수)
  - `buildH7Table(rows: H7SqlRow[], meta: { generatedAt: string; raceDateFrom: number; raceDateTo: number }): H7Table` — 검증 실패 시 throw

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// src/engine/eval/h7Table.test.ts
import { describe, it, expect } from 'vitest';
import { buildH7Table, GAP_BUCKETS, ACHIEVE_BUCKETS, type H7SqlRow } from './h7Table.js';

const SQL_GAP = ['a. ~0.5초', 'b. ~1.0초', 'c. ~1.5초', 'd. 1.5초+'];
const SQL_ACH = ['1_낮음(~30%)', '2_중간(30~70%)', '3_높음(70%+)'];

function fullRows(): H7SqlRow[] {
  const rows: H7SqlRow[] = [];
  for (const g of SQL_GAP) for (const a of SQL_ACH) {
    rows.push({ gapBucket: g, achieveBucket: a, starts: 100, winRate: 0.1, placeRate: 0.3 });
  }
  return rows;
}
const META = { generatedAt: '2026-07-18', raceDateFrom: 20220101, raceDateTo: 20260626 };

describe('buildH7Table', () => {
  it('SQL 접두 라벨 → 클린 라벨 매핑 + 12칸 + totalStarts 합산', () => {
    const t = buildH7Table(fullRows(), META);
    expect(t.cells).toHaveLength(12);
    expect(t.totalStarts).toBe(1200);
    expect(t.cells.map((c) => c.gapBucket)).toEqual(expect.arrayContaining([...GAP_BUCKETS]));
    expect(t.cells.map((c) => c.achieveBucket)).toEqual(expect.arrayContaining([...ACHIEVE_BUCKETS]));
    expect(t.cells.some((c) => (c.gapBucket as string).includes('a.'))).toBe(false);
    expect(t.generatedAt).toBe('2026-07-18');
  });
  it('칸 누락 시 throw', () => {
    expect(() => buildH7Table(fullRows().slice(0, 11), META)).toThrow(/12/);
  });
  it('중복 칸 throw', () => {
    const rows = fullRows(); rows[1] = { ...rows[0] };
    expect(() => buildH7Table(rows, META)).toThrow();
  });
  it('rate 범위 밖·starts 0 throw', () => {
    const bad = fullRows(); bad[0] = { ...bad[0]!, winRate: 1.2 };
    expect(() => buildH7Table(bad, META)).toThrow();
    const zero = fullRows(); zero[0] = { ...zero[0]!, starts: 0 };
    expect(() => buildH7Table(zero, META)).toThrow();
  });
  it('미지 라벨 throw', () => {
    const bad = fullRows(); bad[0] = { ...bad[0]!, gapBucket: 'e. 2초+' };
    expect(() => buildH7Table(bad, META)).toThrow();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run src/engine/eval/h7Table.test.ts`
Expected: FAIL — "Cannot find module './h7Table.js'"

- [ ] **Step 3: 빌더 구현**

```ts
// src/engine/eval/h7Table.ts
/**
 * H7 교차표 빌더 — probe H9 SQL 결과 행을 검증·클린 라벨로 변환해 정적 JSON 구조를 만든다.
 * 소비처: scripts/export_h7_table.ts → client/src/data/h7_table.json → /insights 페이지.
 * 스펙: docs/superpowers/specs/2026-07-18-h7-insights-design.md §2
 */
export const GAP_BUCKETS = ['~0.5초', '~1.0초', '~1.5초', '1.5초+'] as const;
export const ACHIEVE_BUCKETS = ['낮음(~30%)', '중간(30~70%)', '높음(70%+)'] as const;
export type GapBucket = (typeof GAP_BUCKETS)[number];
export type AchieveBucket = (typeof ACHIEVE_BUCKETS)[number];

// probe H9 SQL의 정렬용 접두 라벨 → 클린 라벨
const GAP_FROM_SQL: Record<string, GapBucket> = {
  'a. ~0.5초': '~0.5초', 'b. ~1.0초': '~1.0초', 'c. ~1.5초': '~1.5초', 'd. 1.5초+': '1.5초+',
};
const ACH_FROM_SQL: Record<string, AchieveBucket> = {
  '1_낮음(~30%)': '낮음(~30%)', '2_중간(30~70%)': '중간(30~70%)', '3_높음(70%+)': '높음(70%+)',
};

export interface H7SqlRow {
  gapBucket: string;
  achieveBucket: string;
  starts: number;
  winRate: number;   // 0~1
  placeRate: number; // 0~1
}

export interface H7Cell {
  gapBucket: GapBucket;
  achieveBucket: AchieveBucket;
  starts: number;
  winRate: number;
  placeRate: number;
}

export interface H7Table {
  generatedAt: string;
  raceDateFrom: number;
  raceDateTo: number;
  totalStarts: number;
  formula: string;
  cells: H7Cell[];
}

const FORMULA =
  '격차=예측 G3F 시간차(as-of 이력) · 달성확률=필요 종반속도 달성 확률(개체 이력 정규근사)';

export function buildH7Table(
  rows: H7SqlRow[],
  meta: { generatedAt: string; raceDateFrom: number; raceDateTo: number }
): H7Table {
  const seen = new Set<string>();
  const cells: H7Cell[] = rows.map((r) => {
    const gap = GAP_FROM_SQL[r.gapBucket];
    const ach = ACH_FROM_SQL[r.achieveBucket];
    if (!gap || !ach) throw new Error(`미지 버킷 라벨: ${r.gapBucket} / ${r.achieveBucket}`);
    const key = `${gap}|${ach}`;
    if (seen.has(key)) throw new Error(`중복 칸: ${key}`);
    seen.add(key);
    if (!(r.starts > 0)) throw new Error(`starts 비정상 (${key}): ${r.starts}`);
    for (const [name, v] of [['winRate', r.winRate], ['placeRate', r.placeRate]] as const) {
      if (!(v >= 0 && v <= 1)) throw new Error(`${name} 범위 밖 (${key}): ${v}`);
    }
    return { gapBucket: gap, achieveBucket: ach, starts: r.starts, winRate: r.winRate, placeRate: r.placeRate };
  });
  if (cells.length !== 12) throw new Error(`칸 수 ${cells.length} ≠ 12 (격차 4 × 달성확률 3)`);
  return {
    ...meta,
    totalStarts: cells.reduce((s, c) => s + c.starts, 0),
    formula: FORMULA,
    cells,
  };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/engine/eval/h7Table.test.ts`
Expected: 5 tests PASS

- [ ] **Step 5: export 스크립트 작성**

```ts
// scripts/export_h7_table.ts
/**
 * H7 교차표 정적 JSON 생성 — probe H9(scripts/probe_race_shape.ts 390~440행) SQL을 그대로
 * 복사해 로컬 DuckDB(READ_ONLY)에서 실행, buildH7Table 검증 후 client/src/data/h7_table.json 출력.
 * probe 원본은 수정하지 않는다. 갱신: npm run export:h7 (수동 — 재학습 사이클 등).
 * KRA API·Supabase 호출 0.
 */
import { DuckDBInstance } from '@duckdb/node-api';
import { writeFileSync } from 'fs';
import { buildH7Table, type H7SqlRow } from '../src/engine/eval/h7Table.js';

const inst = await DuckDBInstance.create('data/local.duckdb', { access_mode: 'READ_ONLY' });
const conn = await inst.connect();

async function q(sql: string): Promise<Record<string, unknown>[]> {
  const r = await conn.run(sql);
  const rows = await r.getRowObjects();
  return rows.map((row) =>
    Object.fromEntries(Object.entries(row).map(([k, v]) => [k, typeof v === 'bigint' ? Number(v) : v])),
  );
}

// ⬇ BASE·CTE 체인: probe_race_shape.ts의 `const BASE`(40행~) 정의와 H9 쿼리(394~440행)를
//   그대로 복사하되 ① `${LEAD}`/`${CHASE}`는 0.25/0.55로 인라인 ② `${pct(...)}` 대신
//   0~1 소수로: `SUM(CASE WHEN ord = 1 THEN 1 ELSE 0 END)::DOUBLE / COUNT(*) AS win_rate`
//   `SUM(CASE WHEN ord <= 3 THEN 1 ELSE 0 END)::DOUBLE / COUNT(*) AS place_rate`
//   ③ SELECT 별칭을 gap_bucket, achieve_bucket, starts(COUNT(*)), win_rate, place_rate로.
const CTE = `WITH /* probe_race_shape.ts BASE+H9 복사본 — 원본 참조 */ ...`; // ← 복사해 완성

const cellRows = await q(`${CTE}
SELECT ... AS gap_bucket, ... AS achieve_bucket, COUNT(*) AS starts,
       SUM(CASE WHEN ord = 1 THEN 1 ELSE 0 END)::DOUBLE / COUNT(*) AS win_rate,
       SUM(CASE WHEN ord <= 3 THEN 1 ELSE 0 END)::DOUBLE / COUNT(*) AS place_rate
FROM scored GROUP BY 1, 2 ORDER BY 1, 2`);

const metaRows = await q(`${CTE}
SELECT MIN(race_date) AS f, MAX(race_date) AS t FROM scored`);

const rows: H7SqlRow[] = cellRows.map((r) => ({
  gapBucket: String(r.gap_bucket),
  achieveBucket: String(r.achieve_bucket),
  starts: Number(r.starts),
  winRate: Number(r.win_rate),
  placeRate: Number(r.place_rate),
}));

const table = buildH7Table(rows, {
  generatedAt: new Date().toISOString().slice(0, 10),
  raceDateFrom: Number(metaRows[0]!.f),
  raceDateTo: Number(metaRows[0]!.t),
});

writeFileSync('client/src/data/h7_table.json', JSON.stringify(table, null, 2) + '\n', 'utf8');
console.log(`✅ h7_table.json 생성 — ${table.totalStarts.toLocaleString()}출주, ${table.raceDateFrom}~${table.raceDateTo}, 12칸`);
```

⚠️ `const CTE = ...` 부분은 반드시 `scripts/probe_race_shape.ts`를 열어 `const BASE`(40행부터 backtick 끝까지)와 H9 쿼리의 CTE 체인(par·dev·h·ranked·scored, 394~431행)을 **그대로 이어붙여** 완성할 것 — 산식(윈도우 함수·필터 `pred_gap > 0 AND n_pred >= 5 AND prior_cnt >= 3`)을 한 글자도 바꾸지 않는다. 버킷 CASE(432~436행)도 probe 그대로(접두 라벨 유지 — 빌더가 매핑).

- [ ] **Step 6: package.json 등록 + 실행 → JSON 생성**

`"export:h7": "tsx scripts/export_h7_table.ts"` 추가 후:

Run: `npm run export:h7`
Expected: `✅ h7_table.json 생성 — N출주, 20220101~2026XXXX, 12칸` (N은 수만 단위). 실패(칸<12 등) 시 SQL 복사 누락 확인.

Run: `head -30 client/src/data/h7_table.json`
Expected: 스키마대로 cells 12개, winRate가 격차↑일수록 대체로 감소(단조 경향 — 스펙 H9 검증 결과와 부합하는지 눈으로 확인, 어긋나면 SQL 복사 오류 의심)

- [ ] **Step 7: 전체 검증 + 커밋**

Run: `npm run build && npm run test:run`
Expected: PASS

```bash
git add src/engine/eval/h7Table.ts src/engine/eval/h7Table.test.ts scripts/export_h7_table.ts package.json client/src/data/h7_table.json
git commit -m "feat(insights): H7 교차표 export — probe H9 SQL 복사·빌더 검증·정적 JSON 생성 (F-004)"
```

---

### Task 2: /insights 페이지 + 라우트 + 헤더 링크 + 문서

**Files:**
- Create: `client/src/pages/Insights.tsx`
- Modify: `client/src/App.tsx` (라우트 추가)
- Modify: `client/src/components/Layout.tsx:15-22` (헤더 링크 — 실험실 옆)
- Modify: `docs/status/06-ui.md`, `TODO.md` (F-004 완료 처리)

**Interfaces:**
- Consumes: `client/src/data/h7_table.json` (Task 1 산출 — H7Table 스키마)

- [ ] **Step 1: Insights 페이지 작성**

```tsx
// client/src/pages/Insights.tsx — 전개 인사이트 (F-004: H7 교차표)
import raw from '../data/h7_table.json';

const GAP_ORDER = ['~0.5초', '~1.0초', '~1.5초', '1.5초+'] as const;
const ACH_ORDER = ['낮음(~30%)', '중간(30~70%)', '높음(70%+)'] as const;

interface H7Cell {
  gapBucket: string; achieveBucket: string; starts: number; winRate: number; placeRate: number;
}
interface H7Table {
  generatedAt: string; raceDateFrom: number; raceDateTo: number;
  totalStarts: number; formula: string; cells: H7Cell[];
}
const h7 = raw as H7Table;

const MIN_N = 30; // 표본 적음 표기 기준

/** 승률에 따른 셀 배경 강도 (다크 테마, 라이브러리 없음) */
function cellBg(winRate: number): string {
  if (winRate >= 0.15) return 'bg-emerald-500/30';
  if (winRate >= 0.1) return 'bg-emerald-500/20';
  if (winRate >= 0.06) return 'bg-emerald-500/10';
  return '';
}

function fmtD(d: number): string {
  return `${Math.floor(d / 10000)}.${Math.floor(d / 100) % 100}.${d % 100}`;
}

export function Insights() {
  const cell = (g: string, a: string) =>
    h7.cells.find((c) => c.gapBucket === g && c.achieveBucket === a);
  return (
    <div className="space-y-6">
      <h1 className="text-xl font-semibold">전개 인사이트</h1>

      <section className="space-y-3">
        <h2 className="text-base font-semibold">격차 × 역전 능력 → 실측 승률 (H7 교차표)</h2>
        <p className="text-sm text-[var(--color-text-secondary)]">
          <b>격차</b> = 경주 전 예측되는 선두와의 G3F(결승 600m 전) 시간차 ·{' '}
          <b>달성확률</b> = 그 격차를 뒤집는 데 필요한 종반 속도를 이 말이 과거 이력상 낼 확률
        </p>
        <div className="overflow-x-auto">
          <table className="w-full text-sm font-mono-num border-collapse">
            <thead>
              <tr className="text-[var(--color-text-secondary)] text-xs">
                <th className="px-2 py-2 text-left">예측 격차 ＼ 달성확률</th>
                {ACH_ORDER.map((a) => (
                  <th key={a} className="px-2 py-2 text-right">{a}</th>
                ))}
              </tr>
            </thead>
            <tbody>
              {GAP_ORDER.map((g) => (
                <tr key={g} className="border-t border-[var(--color-bg-elevated)]">
                  <td className="px-2 py-2 text-[var(--color-text-secondary)]">{g}</td>
                  {ACH_ORDER.map((a) => {
                    const c = cell(g, a);
                    if (!c) return <td key={a} className="px-2 py-2 text-right">-</td>;
                    const dim = c.starts < MIN_N;
                    return (
                      <td key={a} className={`px-2 py-2 text-right ${cellBg(c.winRate)} ${dim ? 'opacity-50' : ''}`}>
                        <div className="font-semibold">승 {(c.winRate * 100).toFixed(1)}%</div>
                        <div className="text-xs text-[var(--color-text-secondary)]">
                          연 {(c.placeRate * 100).toFixed(1)}% · n={c.starts.toLocaleString()}
                          {dim && ' · 표본 적음'}
                        </div>
                      </td>
                    );
                  })}
                </tr>
              ))}
            </tbody>
          </table>
        </div>
        <p className="text-xs text-[var(--color-text-disabled)]">
          표본 {fmtD(h7.raceDateFrom)}~{fmtD(h7.raceDateTo)} · {h7.totalStarts.toLocaleString()}출주 ·
          생성 {h7.generatedAt} · {h7.formula}
        </p>
        <p className="text-xs text-[var(--color-text-disabled)]">
          ⚠️ 이 신호는 v7 모델이 이미 피처(shape_signal)로 학습에 사용 중 — 이 표는 사람용 배경 자료입니다.
        </p>
      </section>
    </div>
  );
}
```

- [ ] **Step 2: 라우트 + 헤더 링크**

`client/src/App.tsx` — 기존 Route 나열에 추가 (import 포함):

```tsx
import { Insights } from './pages/Insights';
// Routes 안, /lab 라우트 근처:
<Route path="/insights" element={<Insights />} />
```

`client/src/components/Layout.tsx` — 헤더 우측을 링크 2개 컨테이너로 (15~22행의 기존 실험실 Link를 감싸는 div 추가):

```tsx
import { NavLink, Outlet, Link } from 'react-router-dom';
import { LayoutDashboard, BarChart3, Settings as SettingsIcon, FlaskConical, Star, TrendingUp } from 'lucide-react';
// 헤더 우측:
<div className="flex items-center gap-2">
  <Link
    to="/insights"
    className="flex items-center gap-1.5 px-2.5 py-1.5 rounded-lg text-xs bg-[var(--color-bg-elevated)] text-[var(--color-text-secondary)] hover:bg-[var(--color-accent-cyan)] hover:text-black transition-colors"
    title="전개 인사이트 — 실측 참고자료"
  >
    <TrendingUp className="w-4 h-4" />
    <span>인사이트</span>
  </Link>
  <Link
    to="/lab"
    className="flex items-center gap-1.5 px-2.5 py-1.5 rounded-lg text-xs bg-[var(--color-bg-elevated)] text-[var(--color-text-secondary)] hover:bg-[var(--color-accent-cyan)] hover:text-black transition-colors"
    title="판단항목 가중치 실험"
  >
    <FlaskConical className="w-4 h-4" />
    <span>실험실</span>
  </Link>
</div>
```

- [ ] **Step 3: 문서**

`TODO.md`의 F-004 항목을:

```markdown
- [x] **F-004 H7 교차표 UI 노출** — 완료 2026-07-18: /insights 전개 인사이트 페이지, 정적 12칸 실측표(export:h7). 말별 매핑은 서버 저장 필요 — 후속 검토
```

`docs/status/06-ui.md` "현재 상태"에 1줄:

```markdown
- **/insights 전개 인사이트 (2026-07-18, F-004)** — H7 12칸 교차표(예측 격차×달성확률→실측 승·연승률) 정적 노출. `export:h7`(probe H9 SQL 복사·로컬 DuckDB)→`client/src/data/h7_table.json` 커밋 방식(런타임 쿼리 0). 갱신은 수동 재실행. 말별 매핑=범위 밖(서버 저장 필요).
```

"다음 후보·남음"에서 H7 교차표 부분 제거(F-005 사후 리뷰는 유지 — 전개 트랙 잔여 후보 줄에서 H7만 삭제, 04-signals.md의 해당 줄도 동일하게).

- [ ] **Step 4: 검증 + 커밋**

Run: `cd client && npx tsc -b && cd .. && npm run test:run`
Expected: 타입 에러 0 + 전체 통과

```bash
git add client/src/pages/Insights.tsx client/src/App.tsx client/src/components/Layout.tsx TODO.md docs/status/06-ui.md docs/status/04-signals.md
git commit -m "feat(insights): /insights 전개 인사이트 페이지 — H7 교차표 렌더 + 헤더 진입 (F-004)"
```

- [ ] **Step 5: 사용자 시각 확인 안내 (보고 항목)**

`npm run client:dev` → 헤더 "인사이트" → 교차표 12칸·히트 강도·표본 적음 표기·메타 문구 확인. 확인 후 main 머지는 사용자 결정.

---

## Self-Review 결과

- **스펙 커버리지**: §2 export 스크립트·스키마·검증·probe 불변(Task 1) / §3 페이지·라우트·헤더·읽는법·메타·주의문·표본30 흐림(Task 2) / §4 빌더 단위테스트 5종(Task 1) / §5 범위 밖 — 말별 매핑·자동 갱신·사후판 미구현, TODO에 후속 명기 ✓
- **플레이스홀더**: Task 1 Step 5의 `const CTE = ...`는 원본 파일 특정 행을 그대로 복사하라는 정확한 전사 지시(산식 무변경 조건 포함) — 창작 여지 없음 ✓. 그 외 전부 실코드 ✓
- **타입 일관성**: `H7SqlRow`·`buildH7Table`(Task 1 정의·소비), JSON 스키마 ↔ Insights.tsx의 로컬 `H7Table` 인터페이스 필드 일치(클라 별도 타입 정의는 서버/클라 분리 패턴 — runningStyle 선례) ✓. 버킷 문자열이 빌더 상수·페이지 ORDER 배열·테스트에서 동일 ✓
