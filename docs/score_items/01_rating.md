# ① 레이팅

**18개 항목 중 비중 (PRD 초기):** 17.54점
**학습 가중치 (2026-05-25):** 5.68점
**최신 학습 ρ:** +0.119
**상태:** ✅ 확정
**최근 업데이트:** 2026-05-26 (PRD 동기화)

> 학습이 가중치를 17.54 → 5.68 로 줄였음. ρ +0.12 는 평범한 신호 강도.
> PRD 초기에 가장 큰 비중이었으나 실측 예측력은 ⑧/③/⑱/⑰/⑨ 보다 낮음.

---

## 📊 KRA API 필드

```
rating: 정수 (0 ~ 150 이론, 실제 ~108)
rankRise: 등급 변동 (양수=상승, 음수=하락)
```

## 🎯 계산

```javascript
function calculateRatingScore(horse) {
  if (horse.rating === 0) return 0; // 6등급 미부여
  return horse.rating / 140;        // PRD v4.0 그대로
}
```

## 📊 실제 데이터 분포 (KRA 검증 결과)

```
서울 경마 최대값: ~108 (2026-04-26 기준)
부산경남 최대값: ~80
일반적 범위: 0~100
6등급 미부여: rating = 0 (다수)
```

## ⚠️ 검증 시 발견 사항

- 실제 최대값이 ~108인데 PRD는 140 만점으로 설정
- 사용자 결정: 그대로 유지 (이론적 최대값)
- 실제로는 100점이 만점에 가까운 효과

## ❌ rankRise 미사용 (2026-05-22 결정)

```
rankRise 필드는 KRA에서 등급 변동(±1) 정보 제공:
  - 대부분의 경주에서 0 (변화 없음)
  - 가끔 ±1 (등급 상승/하락)
  
결정: 점수 계산에 미반영
  - 데이터는 수집 (참고용)
  - 점수에는 반영 안 함
  
이유:
  - 너무 희소한 신호 (대부분 0)
  - 절대 레이팅(rating) 자체가 이미 등급 정보 반영
  - 17개 항목으로 충분
```

## 🔗 의존성

- KRA API: API214_1 (`rating`)
- DB: `race_entries.ratg` (= 구 `horse_results.rating`). rank_str 은 race_entries 에 통합되지 않음 (필요 시 별도 컬럼 추가)

## 📚 변경 이력

| 일자 | 변경 |
|------|------|
| 2026-05-22 | rankRise 미사용 확정 (희소 신호) |
| 2026-05-22 | PRD v4.0 그대로 유지 결정 (140 만점) |
| 2026-05-22 | 실제 데이터 검증 (최대 ~108 확인) |
